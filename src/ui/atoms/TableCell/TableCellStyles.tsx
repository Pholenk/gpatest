import {StyleSheet} from 'react-native';

export const TableCellStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderWidth: 1.5,
    borderColor: '#fafafa',
    flex: 1,
  },
});
